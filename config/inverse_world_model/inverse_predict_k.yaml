dataset_id: D4RL/antmaze/umaze-v1
datasets_path: data/minari

encoder_ckpt: runs_encoder/dual_input_masked/encoder_best.pt
input_mode: full

log_root: runs_inverse_world_model

load_default_checkpoint: true

predict_k: true

k_max: 10
hidden_dim: 512
n_hidden: 3

w_action: 1.0
w_latent: 1.0
w_reward: 1.0
w_xydist: 1.0
w_k: 1.0

batch_size: 256
total_steps: 200000
lr: 3.0e-4
cosine_decay_to: 3.0e-5
weight_decay: 1.0e-5
grad_clip: 20.0

log_every: 200
eval_every: 5000
ckpt_every: 10000
eval_batches: 8
eval_batch_size: 256
eval_k_breakdown: [1, 3, 5, 10]

val_frac: 0.05

seed: 0
device: cuda

wandb: false
wandb_project: MIRAGE
wandb_entity: samratsahoo-stanford-university
